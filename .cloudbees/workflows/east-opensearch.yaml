apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: EAST-OPENSEARCH

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: east-opensearch
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
